<h1>Welcome to Invest In Me</h1>
<ul>
	<% @campaigns.each do |c| %>
		<%= link_to user_campaign_path(@user, c) do %>
			<li><%= c.name %></li>
		<% end %>
	<% end %>
</ul>

<%= link_to new_user_campaign_path(@user) do %>
	<div class="btn floating">
		New campaign
	</div>
<% end %>